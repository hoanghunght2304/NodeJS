extends ../layouts/common

block content

    h1 Create user
    form(action="/users/create", method="POST")
        label(for="name") Name
        input(type="text", id="name", name="name") 
        button Create